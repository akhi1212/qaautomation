environments {
    default {
        serenity.project.name = VIN Description Service
        runtime.crm.base.url = "http://runtime-crm-service-develop.develop.rancher.autodata.tech/"
        vin.id.cassandra.base.url = "http://vinid-cassandra-2-0-2-release.qa1.istest.autodata.tech/"
        vds.base.url = ${APP_URL}
        report.customfields.environment = ${APP_ENVIRONMENT}
        report.customfields.ApplicationVersion = ${APP_VERSION}
        storm.environment = ${STORM_ENVIRONMENT}
    }
}